func # test <- with (int# var1, int# var2) => int do
    print("var1 >> " + var1);
    if (var1) do
        print("If");
    else do
        print("Else");
        test(!!var1, var2);
    done;
done;

bool # test_var <- 10110bu; 
int # p <- !!TT;

test(p, test_var);